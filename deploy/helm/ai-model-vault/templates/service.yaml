apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-model-vault.fullname" . }}
  labels:
    {{- include "ai-model-vault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-model-vault.selectorLabels" . | nindent 4 }}
